#include<stdio.h>

int main()
{
    int a,b;
    char c;
    scanf("%d %c %d",&a,&c,&b);

    if(c=='+')
    {
        printf("%d %c %d= %d",a,c,a+b);
    }
    else if(c=='-')
    {
        printf("%d %c %d= %d",a,c,a-b);
    }
    else if(c=='*')
    {
        printf("%d %c %d= %d",a,c,a*b);
    }
    else(c=='/')
    {
        printf("%d %c %d= %d",a,c,a/b);
    }
//오류가 뜨는 이유찾기
    return 0;
}
